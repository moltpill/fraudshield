# FraudShield Platform — Environment Variables
# Copy this file to .env and fill in your values.
#
#   cp .env.example .env
#   # Edit .env with your real values
#   docker compose up -d

# ── Domain ────────────────────────────────────────────────────────────────────
# Your root domain. Caddy will serve:
#   api.<DOMAIN>    → API (port 3001)
#   app.<DOMAIN>    → Dashboard (port 3002)
#   admin.<DOMAIN>  → Admin panel (port 3003)
DOMAIN=fraudshield.example

# ── Database ──────────────────────────────────────────────────────────────────
# PostgreSQL password for the 'fraudshield' user
POSTGRES_PASSWORD=change_me_in_production

# Full DATABASE_URL (auto-constructed in docker-compose from POSTGRES_PASSWORD)
# Override here if using an external/managed database:
# DATABASE_URL=postgresql://fraudshield:password@host:5432/fraudshield

# ── Redis ─────────────────────────────────────────────────────────────────────
REDIS_PASSWORD=change_me_in_production

# ── Dashboard (app.<DOMAIN>) ──────────────────────────────────────────────────
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=generate_with_openssl_rand_base64_32

# ── Admin Panel (admin.<DOMAIN>) ──────────────────────────────────────────────
# Generate with: openssl rand -base64 32
ADMIN_NEXTAUTH_SECRET=generate_with_openssl_rand_base64_32

# ── Stitch Express (Billing) ──────────────────────────────────────────────────
# Get these from https://stitch.money/developers
STITCH_CLIENT_ID=
STITCH_CLIENT_SECRET=
STITCH_WEBHOOK_SECRET=

# ── MaxMind GeoLite2 ──────────────────────────────────────────────────────────
# Free license key from https://www.maxmind.com/en/geolite2/signup
MAXMIND_LICENSE_KEY=
