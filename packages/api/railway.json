{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd ../.. && pnpm install --frozen-lockfile && pnpm --filter @fraudshield/sdk build && cd packages/api && npx prisma generate && pnpm run build"
  },
  "deploy": {
    "startCommand": "node packages/api/dist/index.js",
    "restartPolicyType": "ON_FAILURE"
  }
}
