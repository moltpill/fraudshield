{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd ../.. && pnpm install --frozen-lockfile && pnpm --filter @fraudshield/shared build && cd packages/admin && npx prisma generate --schema=../api/prisma/schema.prisma && NEXT_TELEMETRY_DISABLED=1 pnpm run build"
  },
  "deploy": {
    "startCommand": "node .next/standalone/packages/admin/server.js",
    "restartPolicyType": "ON_FAILURE"
  }
}
