{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "nixpacksPlan": {
      "phases": {
        "setup": {
          "nixPkgs": ["nodejs_22", "pnpm-9_x"]
        },
        "install": {
          "cmds": ["cd ../.. && pnpm install --frozen-lockfile"]
        },
        "build": {
          "cmds": ["cd ../.. && pnpm --filter @fraudshield/shared build && cd packages/admin && npx prisma generate --schema=../api/prisma/schema.prisma && NEXT_TELEMETRY_DISABLED=1 pnpm run build && cp -r .next/static .next/standalone/.next/static"]
        }
      },
      "start": {
        "cmd": "node .next/standalone/packages/admin/server.js"
      }
    }
  },
  "deploy": {
    "restartPolicyType": "ON_FAILURE"
  }
}
