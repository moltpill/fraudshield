{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd ../.. && pnpm install --frozen-lockfile && pnpm --filter @fraudshield/shared build && cd packages/dashboard && npx prisma generate --schema=../api/prisma/schema.prisma && NEXT_TELEMETRY_DISABLED=1 pnpm run build"
  },
  "deploy": {
    "startCommand": "node .next/standalone/packages/dashboard/server.js",
    "restartPolicyType": "ON_FAILURE"
  }
}
